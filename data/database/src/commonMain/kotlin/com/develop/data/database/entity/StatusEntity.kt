package com.develop.data.database.entity

import androidx.room.Entity
import androidx.room.PrimaryKey

/**
 * Сущность статуса заметки в базе данных.
 *
 * Статусы позволяют пользователю категоризировать заметки по состоянию выполнения.
 * Пользователь может создавать собственные статусы с уникальными названиями, иконками и цветами.
 *
 * ## Примеры статусов:
 * - "Done" (✅, зелёный) — выполненные задачи
 * - "In Progress" (🔄, синий) — задачи в работе
 * - "Waiting" (⏳, оранжевый) — отложенные задачи
 *
 * ## Использование:
 * ```kotlin
 * val status = StatusEntity(
 *     name = "Done",
 *     icon = "✅",
 *     color = 0xFF4CAF50.toInt(), // зелёный ARGB
 *     sortOrder = 0
 * )
 * statusDao.insert(status)
 * ```
 *
 * @property id Уникальный идентификатор статуса (автогенерируемый).
 * @property name Название статуса, отображаемое пользователю (например, "Done", "In Progress").
 * @property icon Emoji или название иконки для визуального отображения (например, "✅", "🔄").
 *               Может быть null, если иконка не задана.
 * @property color Цвет статуса в формате ARGB (например, 0xFF4CAF50 для зелёного).
 *                Может быть null, если цвет не задан — используется цвет по умолчанию.
 * @property sortOrder Порядок отображения статуса в списке выбора.
 *                    Меньшее значение = выше в списке.
 */
@Entity(tableName = "statuses")
data class StatusEntity(
    @PrimaryKey(autoGenerate = true)
    val id: Long = 0L,

    val name: String,

    val icon: String? = null,

    val color: Int? = null,

    val sortOrder: Int = 0,
)
