package com.develop.core.model

/**
 * Доменная модель статуса заметки.
 *
 * Статус позволяет категоризировать заметки по состоянию выполнения.
 * Пользователь может создавать собственные статусы с уникальными названиями,
 * иконками (emoji) и цветами.
 *
 * ## Примеры использования:
 * ```kotlin
 * // Создание статуса
 * val doneStatus = Status(
 *     id = 1L,
 *     name = "Done",
 *     icon = "✅",
 *     color = 0xFF4CAF50.toInt()
 * )
 *
 * // Отображение в UI
 * Text(
 *     text = "${status.icon ?: ""} ${status.name}",
 *     color = status.color?.let { Color(it) } ?: LocalContentColor.current
 * )
 * ```
 *
 * @property id Уникальный идентификатор статуса.
 * @property name Название статуса (например, "Done", "In Progress").
 * @property icon Emoji-иконка статуса (например, "✅", "🔄"). Может быть null.
 * @property color Цвет статуса в формате ARGB. Может быть null — используется цвет по умолчанию.
 * @property sortOrder Порядок отображения в списке выбора статусов.
 */
data class Status(
    val id: Long = 0L,
    val name: String,
    val icon: String? = null,
    val color: Int? = null,
    val sortOrder: Int = 0,
)
